nderscore.js 파일을 만든다.

underscore.js 파일에 해당 코드를 입력한다.


보통, 다른 사람이 만든 모듈을 사용할 때에는, 실행법이 적혀있는데, (해당 모듈의 홈페이지에) 그렇지 않은 경우에는, module 폴더에 이름을 적어주면 된다.

→ `require('underscore')` 

새로운 배열인, `arr` 을 만들고, 배열에 첫 번째 값인 '3'을 뽑아보도록 하자.

보통, `arr[0]` 을 통해서 출력하지만, "**underscore 모듈"**에서는 `_.first(arr)` 으로 배열의 첫 번째 값을 출력할 수 있다.

!https://prod-files-secure.s3.us-west-2.amazonaws.com/a156cc82-d201-4160-ac40-16f8ab23f7dc/9c350e5d-e7e9-4aba-a673-cf40161eb31b/Untitled.png

node.js 를 실행해보면, 같은 값이 나온다는 것을 통해서 두 개의 코드가 같은 기능을 한다는 것을 파악할 수 있다.

그렇다면, 배열의 마지막 값인 '12'를 뽑아보도록 하자.

보통, `arr[arr.length-1]` 과 같은 복잡한 코드로 뽑아야하지만, underscore 모듈을 활용하면, 

`_.last(arr)` 로도 해당 값을 가져올 수 있다.

!https://prod-files-secure.s3.us-west-2.amazonaws.com/a156cc82-d201-4160-ac40-16f8ab23f7dc/8a1c8d14-7751-4a82-9b16-5757d7b313a8/Untitled.png

**이처럼, underscore 모듈을 활용하면 조금 더 간단한 코드로 내가 원하는 값을 출력하도록 만들 수 있다.**
